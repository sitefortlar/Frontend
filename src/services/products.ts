// Re-export types from the types directory
export { Product, Category, Subcategory, Kit, SortOption, PriceType } from '@/types/Product';

// Re-export data from the data directory
export { products, categories, productLines } from '@/data/products';
